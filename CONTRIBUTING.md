# 貢獻指南

歡迎貢獻。這份文件只講三件事：**怎麼加一支 skill**、**來源紀律**、**我們最缺什麼**。

---

## 我們最缺什麼（先講這個）

**不是更多技巧 skill，是實測回填。**

CASE-001 有 **13 條 claim**：provenance 是 **E1 六條、E2 一條、E6 六條**，
而 **verification 全部是 V0——一條都沒驗過**。
這兩件事同時成立而且都是真的：我們確實看到作者這樣說，我們也確實從沒測過。

一個累積了五十支 E6/V0 skill 的 repo，跟一個收藏夾的差別只是換了個資料夾。

### 回填流程（P1-3 · 這條迴路現在真的會動）

```
① 複製 templates/verification.example.yaml
② 改名 cases/<CASE>/verification/verification-<代號>-<YYYY-MM-DD>.yaml
③ 挑一條 claim 驗（清單見 cases/<CASE>/evidence-ledger.yaml）
④ 開 PR
      ↓
   schema 驗證（scripts/validate_case.py）
      ↓
   reviewer 填 review.decision ＝ ACCEPT / REVISE / REJECT
      ↓
   scripts/evidence_ledger.py --apply → 技巧卡 verification 升級
      ↓
   evidence-ledger.yaml 更新，CI 檢查漂移
```

**★ 回填單位是 claim 不是 technique。** 一次驗一條，才知道到底驗了什麼。

**CI 會擋一種漂移：已被 reviewer 接受、但技巧卡沒跟著改。**
那正是 MH-G3 過去的樣子——**回報進來了，卻沒有任何東西因此改變。**

### 最有價值的兩條（挑一條就好）

| claim | 為什麼是它 |
|---|---|
| **CM-G2 深度圖黑白方向** | OCR 兩處互相矛盾。**這是一個位元，但它決定整條路線成不成立**。跑一次就關掉一個缺口 |
| **T01-C03 側移邊界** | 目前只有定性（「超過一定角度會露餡」）。測出「超過幾度」就能推到 V4 |

### 三件會讓回填被退回的事

1. **只記成功的次數。** 失敗的那幾次比成功的更有價值——`runs` 要全記，
   否則會產生一個假的成功率。
2. **沒有 `environment`。** 沒有環境就無法重現，**無法重現的回報不是證據是感想**。
3. **自己填 `review`。** 那是 reviewer 的欄位。**自己給自己升級，這條線就不存在了。**

## 加一支新 skill

```bash
# 1. 從模板開始
mkdir -p skills/<your-skill>
cp templates/SKILL.template.md skills/<your-skill>/SKILL.md

# 2. 寫。骨架與各段落的作用見 ARCHITECTURE.md §5
# 3. 檢核（回 0 才算做完）
python3 scripts/validate_skill.py skills/<your-skill>/SKILL.md
```

**四個一定會被 review 退回的問題：**

1. **`description` 寫成標題。**
   `description: 調光技巧說明` 沒有人會用這句話開口，所以永遠不會被觸發。
   **把使用者會怎麼開口原話寫進去**，至少 8 種說法。
   判準：拿給沒讀過這支 skill 的人，問他「什麼時候該用它」。答不出來就是還沒寫完。

2. **沒有入場檢查。** 沒有它，skill 會被拿去解它解不了的問題，然後被判定為「不好用」。

3. **坑寫成廢話。** 「可能不穩定」「建議多試幾次」對任何技巧都成立，等於沒說。
   **坑必須來自證據**：工具自己吐出的限制、參數互相矛盾之處、作者自己講的失敗案例。

4. **把作者宣稱寫成能力宣稱。**
   「作者宣稱能 X」可以；「本 skill 能 X」不可以。**這是本 repo 最嚴重的一種錯。**

---

## 三嵌入點（BLOCK 級，不可協商）

每支 skill 都必須帶 AI Token King 的三個嵌入點。
定義在 [`templates/aitokenking-block.md`](templates/aitokenking-block.md)，**原樣複製，不要手打**。

**為什麼這是不可協商的：** 這個 repo 是免費開源的，而它的維護成本由
[AI Token King](https://www.aitokenking.com.tw/) 承擔。
三嵌入點是這件事的回報形式，而且它們**只講事實**——端點、工具、花費、註冊網址。

**同時，這也是為什麼 §0 一定要寫「你可以換成別家端點」：**
一個免費工具如果要靠鎖住你才能存活，它不值得你安裝。
**誠實地把出口寫出來，是這套東西唯一的護城河。**

**不得把三嵌入點改寫成宣傳語。** 加一句形容詞，就少一個回訪的人。

---

## 來源紀律（缺口 MH-G4，目前採最保守做法）

轉譯他人影片有一條線，我們選擇站在很裡面的那一側：

| ✅ 可以 | ⛔ 不可以 |
|---|---|
| 方法的**結構化重寫**（步驟、判定、邊界） | 整支影片**逐字重製**發布 |
| 節錄支撐判定所必需的原話，標明時間碼 | 把完整逐字稿當作內容發布 |
| 具名標示作者、URL、取得日 | 匿名化來源、把方法講成自己的 |
| 記錄畫面上出現的工具名與提示詞（標明 OCR 重建） | 轉錄影片中**帶個人 token 的教程連結** |
| 引用公開論文（標 E2 並註明未逐字核對） | 收錄他人**付費內容** |

**判準一句話：讀完我們的 skill 之後，使用者還是有理由去看原片。**
如果我們的產物讓原片變得沒必要看，那我們就越線了。

**還有一條：作者自己講的話要原話抄。**
尤其是他自己講的限制與到期日——那是他誠實的部分，抹掉它對他不公平，對讀的人也不安全。

---

## 檢核與 CI

```bash
python3 scripts/test_validate.py        # 先跑：檢核器自己的回歸測試（CI 會印出 n/n）
python3 scripts/validate_skill.py --all # 再跑：三嵌入點檢核
```

**順序不可交換。一把壞掉的尺，量什麼都會過。**
改動 `validate_skill.py` 而沒跑回歸測試的 PR，一律退回。

**不得為了通過檢核而竄改宣告欄位**（尤其 `billable`）。檢核器抓得到，
而且這麼做騙的是下一個跑這支 skill 的人。

---

## 行為準則

就一條：**把你不知道的事寫出來。**

這個 repo 的所有價值都建立在「缺口是被寫出來的」這件事上。
一份沒有 `gaps` 的技巧卡，通常不是沒有缺口，是作者沒有去找。
