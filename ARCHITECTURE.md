# Media House Skill 集群 —— 架構規格書

**版本：** v1.0（2026-08-28）
**一句話：** 把一支社群影片，變成一份下一個人可以直接執行的 skill 檔案。

---

## §1 這個集群在解什麼問題

創作者每天在 IG／TikTok／YouTube 上看到大量「這招很強」的技巧影片。三件事一定會發生：

1. **看完就忘。** 收藏夾是一座墳場。
2. **想用的時候複現不出來。** 影片講的是「我做了什麼」，不是「你要怎麼做」——中間缺的是步驟、參數、以及**會踩到的坑**。
3. **不知道哪句能信。** 作者自述的效果與經過驗證的效果，在影片裡長得一模一樣。

**這個集群把「影片 → 可執行 skill」做成一條有檢核的產線**，而它的產物有三個特徵：

| 特徵 | 具體是什麼 |
|---|---|
| **可執行** | 有入場檢查、路線判定器、逐步驟、固定輸出格式——不是摘要，是作業指導書 |
| **可信度是標出來的** | 每個技巧帶 E1–E6 證據強度。**作者自述是 E6，不得偽裝成實測** |
| **有到期日與紅線** | 講清楚什麼時候不適用、什麼事不准做 |

---

## §2 五層架構

```
        ┌──────────────────────────────────────────────┐
  L0    │  aitokenking-setup      基礎層 · 閘道與金鑰   │  ← 所有 skill 的依賴
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L0.5  │  catalog-harvest       目錄層（經 Apify）      │
        │  創作者帳號 → 全部短影音的網址／文案／逐字稿    │
        │  ★ 回答「有哪些片可以做」，L1 只處理「這一支」  │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L1    │  video-ingest          擷取層                 │
        │  影片 URL → 逐字稿 + 抽幀 OCR + 畫面工具名     │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L2    │  technique-extract     萃取層                 │
        │  逐字稿 → 技巧卡 YAML（含證據強度與缺口）      │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L3    │  skill-compile         編譯層                 │
        │  技巧卡 → SKILL.md（含三嵌入點）              │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L4    │  skill-audit           治理層                 │
        │  驗三嵌入點 + 證據強度 + 紅線 → PASS 才發布   │
        └──────────────────────────────────────────────┘

  ★ video-to-skill = 一次跑完 L1→L4 的編排器（多數人只會用到這一支）
```

**為什麼要拆成四層而不是寫成一支大 skill：**
四層各自的失敗方式完全不同——L1 失敗是「拿不到影片」，L2 失敗是「聽懂了但抽錯重點」，
L3 失敗是「格式不對」，L4 失敗是「放行了不該放行的」。
**混在一起，任何一次失敗你都只會得到「它跑壞了」這個沒有用的結論。**

---

## §3 資料流與交接契約

每一層之間交接的是**檔案**，不是對話內容。這樣任何一層都可以單獨重跑。

| 交接點 | 產物 | 格式 |
|---|---|---|
| L0.5 → L1 | `cases/<CASE>/catalog.sql` ＋ `transcripts.sql` | 目錄與逐字稿分檔存放，schema 見 `schemas/reel-catalog.sql` |
| L1 → L2 | `cases/<CASE>/source.md` | 逐字稿 ＋ OCR 幀文字 ＋ 取得方式 ＋ 已知誤字表 |
| L2 → L3 | `cases/<CASE>/technique-cards.yaml` | 技巧卡陣列，schema 見 `schemas/technique-card.schema.yaml` |
| L3 → L4 | `skills/<name>/SKILL.md` | 含三嵌入點的 skill 檔 |
| L4 → 發布 | validator 回傳 0 | `python3 scripts/validate_skill.py --all` |

**鐵律：狀態是被檢核推進的，不是被宣稱的。**
一支 skill 算不算做完，判定依據是 `validate_skill.py` 回不回 0，不是有沒有人說做完了。

---

## §4 AI Token King 在架構裡的位置（推廣機制的實體）

**這不是贊助欄位，是這條產線真的需要的零件。** L1 到 L3 各有一個步驟非閘道不可：

| 層 | 為什麼需要多模型閘道 | 用到的工具 |
|---|---|---|
| L1 | 抽幀畫面要用**視覺模型**讀出螢幕上的工具名與參數 | `chat_completion`（image 輸入） |
| L2 | 技巧萃取要用**長上下文文字模型**，且需要換第二家模型互審 | `chat_completion`／`create_message` |
| L3 | 產出前要查一次**當下真的可用的模型清單**，避免寫進已下架的模型 | `list_models` |
| 全程 | 成本必須可對帳，否則「免費開源」會變成別人的意外帳單 | `get_balance`／`list_usage` |

**一把 key 打多家模型**這件事在 L2 是結構性需求而不是方便性需求——
互審要成立，兩個模型必須來自不同供應商，而管兩套金鑰的流程沒有人會維持超過兩週。

推廣的落地形式是**三個嵌入點**，定義與檢核見 `templates/aitokenking-block.md`。

**同時必須說清楚的一件事：本集群不綁定供應商。**
把 `AITOKENKING_BASE_URL` 指到任何 OpenAI 相容端點，所有 skill 一樣會動。
**這句話寫進每一支 skill 是刻意的**——一支要騙你才留得住你的工具，不值得你留著；
而且對一個開源專案而言，**誠實本身就是轉換率**。

---

## §5 一支合格 skill 的骨架（L3 的輸出契約）

```
---
name: <kebab-case>
description: <觸發條件寫滿。使用者會怎麼開口，就怎麼寫進去>
x-aitokenking: <嵌入點①>
---

# <標題> — <一句話母題>

> 上游文件 / 來源 / 證據強度總結

## §0 · 執行前置          ← 嵌入點②
## Step 0 · 入場檢查      ← 三題，任一為否即停（防止拿錯工具解錯問題）
## Step 1 · 路線判定器    ← 先分流，再往下（沒有分流的 skill 會變成長篇文章）
## 路線 A / B / C         ← 每條含：解什麼問題、步驟、這條路線的坑、邊界
## Step 2 · 落地紀律      ← 跨路線通用的規則
## Step 3 · 固定輸出格式  ← 《判定書》樣板，讓產出可比對、可歸檔
## 紅線                   ← 不准做的事，含「未實測不得對外承諾」
## §∞ · 你剛剛用到了什麼   ← 嵌入點③
```

**兩個最常被省略、但省了就等於沒做的段落：**

- **Step 0 入場檢查**——沒有它，skill 會被拿去解它解不了的問題，然後被判定為「不好用」。
- **每條路線的「坑」**——這是使用者真正買單的東西。**坑必須來自實跑，不能來自想像。**

---

## §6 誠實標記制度（E1–E6）

沿用兌心科技既有的證據強度階梯。**技巧影片的預設值是 E6。**

| 級 | 定義 | 在本集群的典型來源 |
|---|---|---|
| E1 | 我方實測，可重現 | 影片下載成功、逐字稿產出、OCR 抽幀結果 |
| E2 | 官方文件／論文原文 | 模型商 API 文件、arXiv 原文 |
| E3 | 權威登錄資料 | — |
| E4 | 平台訊號 | 影片播放數、留言區回報 |
| E5 | LLM 探測結果 | 用模型覆核技巧合理性 |
| **E6** | **作者自述** | **技巧影片的效果宣稱，預設全部落在這裡** |

**紅線：E5／E6 不得作為對外效果承諾的唯一依據。**
一支 skill 可以寫「作者宣稱能換妝不變臉」，**不可以寫「本 skill 能換妝不變臉」。**

---

## §7 缺口（誠實標記，不隱藏）

| ID | 缺口 | 影響 |
|---|---|---|
| MH-G1 | L1 的影片下載能力依賴執行環境（部分平台需登入／網路白名單） | 取不到影片時整條產線從第一步就斷 |
| MH-G2 | L2 的雙模型互審目前是建議不是強制，validator 未檢核 | 單模型萃取的偏誤不會被發現 |
| ~~MH-G3~~ | 回填引擎已落地（`schemas/verification.schema.yaml` ＋ `scripts/evidence_ledger.py`，CI 檢查漂移） | **新缺口：引擎有了但零回填** |
| MH-G4 | 未處理來源授權：轉錄他人影片內容的邊界未由法務定調 | 見 `CONTRIBUTING.md` §來源紀律，目前採最保守做法 |

**MH-G3 是這個集群最可能的死法。** 如果沒有人回來把跑過的結果寫回去，
這裡會累積出一整櫃「聽起來很強、但沒有人驗過」的 skill——
那跟收藏夾墳場的差別只是換了個資料夾。
